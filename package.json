{
  "name": "IonicMeteorPost",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --driver-package velocity:html-reporter --port 3100"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}