{
  "name": "IonicMeteorPost",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --driver-package practicalmeteor:mocha --port 3100",
    "testfull": "meteor test --full-app --driver-package practicalmeteor:mocha --port 3100"
  },
  "dependencies": {
    "angular": "^1.5.5",
    "angular-ecmascript": "^0.0.3",
    "meteor-node-stubs": "~0.2.0"
  },
  "devDependencies": {
  }
}